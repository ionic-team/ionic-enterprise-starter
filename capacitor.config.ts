import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.enterprisestarter',
  appName: 'Ionic Enterprise Starter',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
